apptainer {
    enabled = true
    autoMounts = true
    temp = true
    cacheDir = "$PWD/container_cache"
    runOptions = '-C --writable-tmpfs'
}

process {
    container = 'library://nikhilshinde0909/rnaseq/lncranalyzer-nf'
}

